-- 1. Create the avalanche database and schema
CREATE DATABASE IF NOT EXISTS avalanche_db;
CREATE SCHEMA IF NOT EXISTS avalanche_schema;

-- 2. Download CSV data 
-- Link to download from https://github.com/https-deeplearning-ai/rapid-prototyping-with-streamlit/blob/master/M1/Lab3/data/customer_reviews.csv

-- 3. Upload CSV data
-- Data > Add Data > Load data into a Table > Browse > Select CSV file
-- Next, in pop-up modal: Select database / Create table
